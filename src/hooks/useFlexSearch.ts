// import { Index } from 'flexsearch'
// import { useMemo } from 'react'

// const index = useMemo(() => {
//   const idx = new Index()

//   Object.keys(map).forEach(k => {
//     idx.add(k, map[k])
//   })

//   return idx
// }, [map])

export default {}
